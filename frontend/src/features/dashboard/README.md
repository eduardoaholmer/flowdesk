# dashboard

Pasta vazia de propósito — nenhuma sprint implementou uma visão de dashboard
ainda (não está no roadmap atual, `docs/08-roadmap.md`). Quando existir, segue
o padrão de `docs/05-frontend.md` §1: `components/`, `hooks.ts`, `api.ts`,
`types.ts`.
