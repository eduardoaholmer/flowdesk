# settings

Pasta vazia de propósito. Configurações de workspace (membros, convites) hoje
vivem em `features/workspaces/`; esta pasta é reservada para preferências que
não se encaixam ali (ex.: preferências pessoais de conta) quando existirem.
Segue o padrão de `docs/05-frontend.md` §1: `components/`, `hooks.ts`,
`api.ts`, `types.ts`.
